#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

int main(int argc, const char* argv[])
{
  if (argc < 2)
    {
      printf("%s", error_message);
      exit(0);
    }
  else
    {
      load_contacts(*argv[1]);
    }

  bool finished = false;
  while (!finished)
    {

    }
  
  return EXIT_SUCCESS;
}

